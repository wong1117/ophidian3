apiVersion: v1
kind: Service
metadata:
  name: {{ include "ophidian.fullname" . }}
  labels:
    {{- include "ophidian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ophidian.selectorLabels" . | nindent 4 }}
